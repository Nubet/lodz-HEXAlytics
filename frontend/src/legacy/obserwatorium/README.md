# Legacy Observatorium Contract

This directory keeps the old ObserwatoriumBRD data contracts and transformers for historical reference.

It is not part of the current frontend runtime or the current backend API contract.

Use the active types and transformers from `src/types` and `src/utils` for ongoing development.

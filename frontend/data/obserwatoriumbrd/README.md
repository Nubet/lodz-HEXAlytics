# ObserwatoriumBRD Data

This application does not use the files in this directory directly at runtime.

These datasets were generated with the legacy scripts in `frontend/scripts/legacy` and were used as an intermediate import source.

The data are loaded into the local database via backend just once, and then used by this frontend via api calls.
